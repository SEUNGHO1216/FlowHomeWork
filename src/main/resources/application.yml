spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:flow
    username: sa
    password:

  h2:
    console:
      enabled: true
      path: /h2-console
      settings:
        web-allow-others: true

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        format_sql: true
